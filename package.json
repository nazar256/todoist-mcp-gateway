{
  "name": "todoist-mcp-gateway",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "description": "Stateless Cloudflare Worker MCP gateway for Todoist with OAuth Authorization Code + PKCE",
  "repository": {
    "type": "git",
    "url": "https://github.com/nazar256/todoist-mcp-gateway"
  },
  "keywords": ["mcp", "todoist", "cloudflare-workers", "oauth", "gateway"],
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "jose": "^6.2.3",
    "zod": "^3.25.76"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260430.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5",
    "wrangler": "^4.86.0"
  }
}
